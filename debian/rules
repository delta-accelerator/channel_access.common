#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1
export PYBUILD_NAME=channel-access.common

export EPICS_BASE=/usr/lib/epics
export EPICS_HOST_ARCH=linux-x86_64

%:
	dh $@ --with python3 --buildsystem=pybuild
